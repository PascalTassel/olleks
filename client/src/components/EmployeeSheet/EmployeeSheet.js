import React from 'react';
import PropTypes from 'prop-types';
import './employeesheet.scss';

function EmployeeSheet() {
  return (
    <>

    </>
  );
}

EmployeeSheet.propTypes = {
};
EmployeeSheet.defaultProps = {
};
export default React.memo(EmployeeSheet);
